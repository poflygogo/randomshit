## 解題紀錄

### v1-1 AC(23ms, 6.7MB)

- 初稿

### v1-2 AC(23ms, 3.4MB)

- 重構方法內容，不再使用 `grouper()`，而是使用 in-place 的做法完成 `swap()` 和 `sort()`

### v1-3 AC(20ms, 3.4MB)

- 調整處理指令的邏輯，若連續出現 2 個 0，就不執行，直接讀取其他指令；若 1 連續出現，就只執行第一次，後續的不再執行。

### v1-4 AC(21ms, 3.4MB)

- 重寫 `merge()` 方法，期望能盡可能的使用 in-place 的作法(實際上並不是)
